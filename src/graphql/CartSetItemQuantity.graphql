mutation CartSetItemQuantity($updateOrderItemId: ID!, $quantity: Int!, $total: Int!) {
	updateOrderItem(id: $updateOrderItemId, data: { Quantity: $quantity, Total: $total }) {
	
		data {

			id
			attributes {
				Quantity
				Total

			}
		}
	}
}
